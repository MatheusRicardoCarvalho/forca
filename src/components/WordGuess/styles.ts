import { StyleSheet } from "react-native";

export const WordGuessStyle = StyleSheet.create({
    container: {
        flexDirection: 'row',
        justifyContent: 'center',
        marginTop: 20,
    },
    letra: {
        fontSize: 32,
        marginHorizontal: 5,
        fontWeight: 'bold',
        color: '#000',
    },
})